- Add an export and import option that shows text to copy: a list that contains the information of the graph. Copying it, clicking import, and pasting it there, should generate the same graph.

- Option to show vertex degree inside the vertex, enabled by default.
